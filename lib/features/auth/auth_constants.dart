// shared constants for auth feature

/// the length of one-time password code expected by backend.
/// change this value if server requirements change (e.g. 4, 5, or 6 digits)
const int kOtpCodeLength = 4;
